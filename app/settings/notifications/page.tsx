export default function NotificationSettings(){return <div><h2 className="display" style={{fontSize:'2.5rem',marginTop:0}}>Notifications</h2><div className="panel"><p className="muted">Notification preferences activate when email delivery is connected in Supabase. In-app notifications remain available.</p>{['Follow requests and approvals','Review and list interactions','Release reminders','Recommendation digest'].map(x=><label className="field" key={x} style={{gridTemplateColumns:'1fr auto',padding:'12px 0',borderBottom:'1px solid var(--line)'}}><span>{x}</span><input type="checkbox" defaultChecked/></label>)}</div></div>}
